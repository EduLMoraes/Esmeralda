name: Rust

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Install gdk-3 and javascriptcoregtk-4.1
      run: |
        sudo apt-get update
        sudo apt-get install -y libgtk-3-dev libjavascriptcoregtk-4.0-dev  # Verifique o nome exato da biblioteca para sua versão específica
    - name: Run tests
      run: cargo test --verbose
